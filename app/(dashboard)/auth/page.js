// Auth es una página dentro de dashboard, se pone entre parentesis para no ser tenido en cuenta
// dentro de la ruta de la página, por lo que no se podrá acceder a ella directamente.
// Podemos crear varias páginas y estar agrupadas en dashboard sin la necesidad de utilizar la ruta /dashboard

import React from "react";

const AuthPage = () => {
  return <div>AuthPage</div>;
};

export default AuthPage;
